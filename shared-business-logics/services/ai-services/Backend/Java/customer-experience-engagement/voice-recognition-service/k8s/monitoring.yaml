apiVersion: v1
kind: ServiceMonitor
metadata:
  name: voice-recognition-service
  namespace: ai-services
  labels:
    app: voice-recognition-service
    version: v1
    team: team-ai-customer-experience
    release: prometheus
spec:
  selector:
    matchLabels:
      app: voice-recognition-service
  endpoints:
  - port: management
    path: /actuator/prometheus
    interval: 30s
    scrapeTimeout: 10s
    scheme: http
    relabelings:
    - sourceLabels: [__meta_kubernetes_pod_name]
      targetLabel: pod
    - sourceLabels: [__meta_kubernetes_pod_node_name]
      targetLabel: node
    - sourceLabels: [__meta_kubernetes_namespace]
      targetLabel: namespace
  namespaceSelector:
    matchNames:
    - ai-services
---
apiVersion: v1
kind: PodMonitor
metadata:
  name: voice-recognition-service
  namespace: ai-services
  labels:
    app: voice-recognition-service
    version: v1
    team: team-ai-customer-experience
spec:
  selector:
    matchLabels:
      app: voice-recognition-service
  podMetricsEndpoints:
  - port: management
    path: /actuator/prometheus
    interval: 30s
    relabelings:
    - sourceLabels: [__meta_kubernetes_pod_name]
      targetLabel: pod
    - sourceLabels: [__meta_kubernetes_pod_node_name]
      targetLabel: node
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: voice-recognition-service-metrics-reader
  labels:
    app: voice-recognition-service
    team: team-ai-customer-experience
rules:
- nonResourceURLs: ["/metrics", "/actuator/prometheus"]
  verbs: ["get"]
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: voice-recognition-service-grafana-dashboard
  namespace: ai-services
  labels:
    app: voice-recognition-service
    version: v1
    team: team-ai-customer-experience
    grafana_dashboard: "1"
data:
  voice-recognition-service-dashboard.json: |
    {
      "dashboard": {
        "title": "Voice Recognition Service Dashboard",
        "tags": ["ai-services", "voice-recognition", "customer-experience"],
        "timezone": "UTC",
        "panels": [
          {
            "title": "Recognition Request Rate",
            "targets": [{
              "expr": "rate(http_server_requests_seconds_count{application=\"voice-recognition-service\"}[5m])"
            }]
          },
          {
            "title": "Error Rate",
            "targets": [{
              "expr": "rate(http_server_requests_seconds_count{application=\"voice-recognition-service\",status=~\"5..\"}[5m])"
            }]
          },
          {
            "title": "Response Time",
            "targets": [{
              "expr": "histogram_quantile(0.95, rate(http_server_requests_seconds_bucket{application=\"voice-recognition-service\"}[5m]))"
            }]
          },
          {
            "title": "JVM Memory Usage",
            "targets": [{
              "expr": "jvm_memory_used_bytes{application=\"voice-recognition-service\"}"
            }]
          }
        ]
      }
    }
