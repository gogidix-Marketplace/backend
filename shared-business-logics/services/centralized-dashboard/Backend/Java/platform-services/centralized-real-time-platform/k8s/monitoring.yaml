apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: centralized-real-time-platform
  namespace: centralized-dashboard
  labels:
    app: centralized-real-time-platform
    team: team-f-centralized-dashboard
spec:
  selector:
    matchLabels:
      app: centralized-real-time-platform
  endpoints:
  - port: http
    path: /actuator/prometheus
    interval: 30s
    scrapeTimeout: 10s
    scheme: http
