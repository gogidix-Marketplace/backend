apiVersion: v1
kind: Secret
metadata:
  name: shared-utilities-secrets
  namespace: gogidix-infrastructure
  labels:
    app: shared-utilities
    component: utilities
    tier: infrastructure
type: Opaque
data:
  # Database credentials (base64 encoded)
  # Default values - should be overridden in production
  db.username: cG9zdGdyZXM=  # postgres
  db.password: cG9zdGdyZXM=  # postgres
  
  # Redis credentials (base64 encoded)
  redis.password: cmVkaXMxMjM=  # redis123
  
  # Security credentials (base64 encoded)
  security.user: YWRtaW4=      # admin
  security.password: YWRtaW4xMjM=  # admin123

---
# Note: In production, secrets should be managed externally
# using tools like HashiCorp Vault, AWS Secrets Manager, etc.
# This file is for development/testing purposes only