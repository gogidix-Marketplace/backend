apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ai-translation-service-ingress
  namespace: ai-services
  labels:
    app: ai-translation-service
    team: team-ai-customer-experience
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    nginx.ingress.kubernetes.io/configuration-snippet: |
      more_set_headers "X-Frame-Options: SAMEORIGIN";
      more_set_headers "X-Content-Type-Options: nosniff";
    nginx.ingress.kubernetes.io/compression-backend: "on"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - ai-translation.gogidix.com
    secretName: ai-translation-service-tls
  rules:
  - host: ai-translation.gogidix.com
    http:
      paths:
      - path: /api/translation(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: ai-translation-service
            port:
              number: 8085
