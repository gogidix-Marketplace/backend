apiVersion: v1
kind: Service
metadata:
  name: lead-generation-ai-service
  namespace: ai-services
  labels:
    app: lead-generation-ai-service
    team: team-f-ai-services
spec:
  type: ClusterIP
  selector:
    app: lead-generation-ai-service
  ports:
  - name: http
    port: 8211
    targetPort: 8211
    protocol: TCP
