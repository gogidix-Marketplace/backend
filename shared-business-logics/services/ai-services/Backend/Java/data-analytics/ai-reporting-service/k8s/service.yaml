apiVersion: v1
kind: Service
metadata:
  name: ai-reporting-service
  namespace: ai-services
  labels:
    app: ai-reporting-service
    team: team-f-ai-services
spec:
  type: ClusterIP
  selector:
    app: ai-reporting-service
  ports:
  - name: http
    port: 8244
    targetPort: 8244
    protocol: TCP
