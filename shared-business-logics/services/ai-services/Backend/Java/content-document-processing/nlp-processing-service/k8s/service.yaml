apiVersion: v1
kind: Service
metadata:
  name: nlp-processing-service
  namespace: ai-services
  labels:
    app: nlp-processing-service
    team: team-f-ai-services
spec:
  type: ClusterIP
  selector:
    app: nlp-processing-service
  ports:
  - name: http
    port: 8226
    targetPort: 8226
    protocol: TCP
