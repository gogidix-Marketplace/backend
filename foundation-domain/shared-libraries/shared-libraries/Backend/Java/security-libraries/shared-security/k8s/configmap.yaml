apiVersion: v1
kind: ConfigMap
metadata:
  name: shared-security-config
  namespace: shared-libraries
data:
  application.yml: |
    gogidix:
      shared-security:
        enabled: true
        config:
          key: value
