apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: api-gateway-service-pdb
  namespace: centralized-dashboard
  labels:
    app: api-gateway-service
    team: team-f-centralized-dashboard
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: api-gateway-service
