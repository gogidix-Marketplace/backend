apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ai-churn-predictionation-service
  namespace: ai-services
  labels:
    app: ai-churn-predictionation-service
    team: team-f-ai-services
spec:
  selector:
    matchLabels:
      app: ai-churn-predictionation-service
  endpoints:
  - port: http
    path: /actuator/prometheus
    interval: 30s
    scrapeTimeout: 10s
    scheme: http
