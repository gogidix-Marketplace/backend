apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ai-feature-extraction-service-ingress
  namespace: ai-services
  labels:
    app: ai-feature-extraction-service
    team: team-ai-mlops
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    nginx.ingress.kubernetes.io/configuration-snippet: |
      more_set_headers "X-Frame-Options: SAMEORIGIN";
      more_set_headers "X-Content-Type-Options: nosniff";
    nginx.ingress.kubernetes.io/compression-backend: "on"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - ai-feature-extraction.gogidix.com
    secretName: ai-feature-extraction-service-tls
  rules:
  - host: ai-feature-extraction.gogidix.com
    http:
      paths:
      - path: /api/feature-extraction(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: ai-feature-extraction-service
            port:
              number: 8080
