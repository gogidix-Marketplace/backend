---
apiVersion: v1
kind: Namespace
metadata:
  name: infra-lock
  labels:
    name: infra-lock
    domain: foundation-domain
    environment: production
spec:
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: infra-lock-network-policy
  namespace: infra-lock
spec:
  podSelector:
    matchLabels:
      namespace: infra-lock
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              namespace: infra-lock
    - from:
        - namespaceSelector:
            matchLabels:
              namespace: infrastructure
    - from:
        - namespaceSelector:
            matchLabels:
              namespace: platform
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              namespace: infra-lock
    - to:
        - namespaceSelector:
            matchLabels:
              namespace: infrastructure
    - to:
        - namespaceSelector:
            matchLabels:
              domain: kube-system
          ports:
            - protocol: TCP
              port: 53
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/cluster: ""
        ports:
            - protocol: TCP
              port: 443
---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: infra-lock-resource-quota
  namespace: infra-lock
spec:
  hard:
    requests.cpu: "1"
    requests.memory: "2Gi"
    limits.cpu: "2"
    limits.memory: "4Gi"
    persistentvolumeclaims: "20Gi"
    pods: "10"
---
apiVersion: v1
kind: LimitRange
metadata:
  name: infra-lock-container-limits
  namespace: infra-lock
spec:
  limits:
    - type: Container
      defaultRequest:
        cpu: "250m"
        memory: "512Mi"
      defaultLimit:
        cpu: "1000m"
        memory: "1Gi"
      maxLimit:
        cpu: "2000m"
        memory: "2Gi"
---
apiVersion: v1
kind: PriorityClass
metadata:
  name: lock-priority-class
  namespace: infra-lock
spec:
  value: 500
  globalDefault: true
  description: "Standard priority for infrastructure lock services"
