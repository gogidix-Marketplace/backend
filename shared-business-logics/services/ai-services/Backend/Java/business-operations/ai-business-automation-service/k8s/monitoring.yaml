apiVersion: v1
kind: ServiceMonitor
metadata:
  name: ai-business-automation-service
  namespace: ai-services
  labels:
    app: ai-business-automation-service
    version: v1
    team: team-ai-business-ops
    component: business-operations
    environment: production
    release: prometheus-operator
spec:
  selector:
    matchLabels:
      app: ai-business-automation-service
  endpoints:
  - port: management
    path: /actuator/prometheus
    interval: 30s
    scrapeTimeout: 10s
    scheme: http
  namespaceSelector:
    matchNames:
    - ai-services
---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: ai-business-automation-service
  namespace: ai-services
  labels:
    app: ai-business-automation-service
    version: v1
    team: team-ai-business-ops
    component: business-operations
    environment: production
    release: prometheus-operator
spec:
  groups:
  - name: ai-business-automation-service
    interval: 30s
    rules:
    - alert: AiBusinessAutomationServiceHighErrorRate
      expr: rate(http_server_requests_seconds_count{app="ai-business-automation-service",status=~"5.."}[5m]) > 0.05
      for: 5m
      labels:
        severity: warning
        team: team-ai-business-ops
      annotations:
        summary: High error rate detected for AI Business Automation Service
        description: "Error rate is {{ $value }} errors/sec"
    - alert: AiBusinessAutomationServiceHighLatency
      expr: histogram_quantile(0.95, rate(http_server_requests_seconds_bucket{app="ai-business-automation-service"}[5m])) > 1
      for: 10m
      labels:
        severity: warning
        team: team-ai-business-ops
      annotations:
        summary: High latency detected for AI Business Automation Service
        description: "P95 latency is {{ $value }} seconds"
    - alert: AiBusinessAutomationServicePodNotReady
      expr: kube_pod_status_ready{app="ai-business-automation-service",namespace="ai-services"} == 0
      for: 5m
      labels:
        severity: critical
        team: team-ai-business-ops
      annotations:
        summary: AI Business Automation Service pod not ready
        description: "Pod {{ $labels.pod }} is not ready"
    - alert: AiBusinessAutomationServiceHighMemoryUsage
      expr: rate(container_memory_working_set_bytes{app="ai-business-automation-service"}[5m]) > 1200000000
      for: 10m
      labels:
        severity: warning
        team: team-ai-business-ops
      annotations:
        summary: High memory usage detected for AI Business Automation Service
        description: "Memory usage is {{ $value }} bytes"
    - alert: AiBusinessAutomationServiceHighCpuUsage
      expr: rate(container_cpu_usage_seconds_total{app="ai-business-automation-service"}[5m]) > 0.8
      for: 10m
      labels:
        severity: warning
        team: team-ai-business-ops
      annotations:
        summary: High CPU usage detected for AI Business Automation Service
        description: "CPU usage is {{ $value }} cores"
