spring:
  application:
    name: user-management-service
  data:
    mongodb:
      uri: mongodb://localhost:27017
      database: gogidix_users

server:
  port: 8082

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.gogidix: DEBUG
