apiVersion: v1
kind: Service
metadata:
  name: stock-service
  namespace: warehousing-core
  labels:
    app: stock-service
    service: stock-service
spec:
  type: ClusterIP
  sessionAffinity: None
  ports:
  - name: http
    port: 8211
    targetPort: http
    protocol: TCP
  - name: management
    port: 8212
    targetPort: management
    protocol: TCP
  selector:
    app: stock-service
---
apiVersion: v1
kind: Service
metadata:
  name: stock-service-headless
  namespace: warehousing-core
  labels:
    app: stock-service
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - name: http
    port: 8211
    targetPort: http
    protocol: TCP
  selector:
    app: stock-service
