apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ai-customer-engagement-service-ingress
  namespace: ai-services
  labels:
    app: ai-customer-engagement-service
    team: team-ai-customer-experience
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/backend-protocol: HTTP
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "300"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "300"
    nginx.ingress.kubernetes.io/rate-limit: "100"
    nginx.ingress.kubernetes.io/limit-rps: "50"
spec:
  tls:
  - hosts:
    - ai-customer-engagement.gogidix.com
    secretName: ai-customer-engagement-service-tls
  rules:
  - host: ai-customer-engagement.gogidix.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ai-customer-engagement-service
            port:
              number: 8080
