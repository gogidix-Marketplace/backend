spring:
  application:
    name: vendor-service-test
  data:
    mongodb:
      host: localhost
      port: 27017
      database: gogidix_ecommerce_test
  kafka:
    bootstrap-servers: localhost:9092

server:
  port: 0

logging:
  level:
    com.gogidix.ecommerce.vendor: DEBUG
