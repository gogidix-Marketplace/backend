# Test Configuration for Conversion Service
spring:
  application:
    name: conversion-service-test
  data:
    mongodb:
      host: localhost
      port: 27017
      database: conversion-test-db
      auto-index-creation: true
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: conversion-test-group
      auto-offset-reset: earliest
spring.redis:
  host: localhost
  port: 6379
  database: 11
server:
  port: 0
logging:
  level:
    com.gogidix.finance.conversion: DEBUG
    org.springframework.kafka: WARN
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
test:
  tenant:
    id: tenant-test-001
