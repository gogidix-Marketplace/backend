apiVersion: v1
kind: ServiceMonitor
metadata:
  name: predictive-analytics-service
  namespace: ai-services
  labels:
    app: predictive-analytics-service
    version: v1
    team: team-ai-data-analytics
    release: prometheus
spec:
  selector:
    matchLabels:
      app: predictive-analytics-service
  endpoints:
  - port: management
    path: /actuator/prometheus
    interval: 30s
    scrapeTimeout: 10s
    scheme: http
    relabelings:
    - sourceLabels: [__meta_kubernetes_pod_name]
      targetLabel: pod
    - sourceLabels: [__meta_kubernetes_pod_node_name]
      targetLabel: node
    - sourceLabels: [__meta_kubernetes_namespace]
      targetLabel: namespace
  namespaceSelector:
    matchNames:
    - ai-services
---
apiVersion: v1
kind: PodMonitor
metadata:
  name: predictive-analytics-service
  namespace: ai-services
  labels:
    app: predictive-analytics-service
    version: v1
    team: team-ai-data-analytics
spec:
  selector:
    matchLabels:
      app: predictive-analytics-service
  podMetricsEndpoints:
  - port: management
    path: /actuator/prometheus
    interval: 30s
    relabelings:
    - sourceLabels: [__meta_kubernetes_pod_name]
      targetLabel: pod
    - sourceLabels: [__meta_kubernetes_pod_node_name]
      targetLabel: node
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: predictive-analytics-service-metrics-reader
  labels:
    app: predictive-analytics-service
    team: team-ai-data-analytics
rules:
- nonResourceURLs: ["/metrics", "/actuator/prometheus"]
  verbs: ["get"]
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: predictive-analytics-service-grafana-dashboard
  namespace: ai-services
  labels:
    app: predictive-analytics-service
    version: v1
    team: team-ai-data-analytics
    grafana_dashboard: "1"
data:
  predictive-analytics-service-dashboard.json: |
    {
      "dashboard": {
        "title": "Predictive Analytics Service Dashboard",
        "tags": ["ai-services", "predictive-analytics", "data-analytics"],
        "timezone": "UTC",
        "panels": [
          {
            "title": "Prediction Rate",
            "targets": [{
              "expr": "rate(prediction_requests_total{application=\"predictive-analytics-service\"}[5m])"
            }]
          },
          {
            "title": "Request Rate",
            "targets": [{
              "expr": "rate(http_server_requests_seconds_count{application=\"predictive-analytics-service\"}[5m])"
            }]
          },
          {
            "title": "Model Accuracy",
            "targets": [{
              "expr": "model_accuracy{application=\"predictive-analytics-service\"}"
            }]
          },
          {
            "title": "JVM Memory Usage",
            "targets": [{
              "expr": "jvm_memory_used_bytes{application=\"predictive-analytics-service\"}"
            }]
          }
        ]
      }
    }
