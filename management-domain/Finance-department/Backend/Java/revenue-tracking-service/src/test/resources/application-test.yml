# Test Configuration for Revenue Tracking Service
spring:
  application:
    name: revenue-tracking-service-test
  data:
    mongodb:
      host: localhost
      port: 27017
      database: revenue-tracking-test-db
      auto-index-creation: true
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: revenue-tracking-test-group
      auto-offset-reset: earliest
spring.redis:
  host: localhost
  port: 6379
  database: 18
server:
  port: 0
logging:
  level:
    com.gogidix.finance.revenuetracking: DEBUG
    org.springframework.kafka: WARN
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
test:
  tenant:
    id: tenant-test-001
