apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: audit-trail-service-ingress
  namespace: transaction-orchestration
  labels:
    app: audit-trail-service
    team: team-b-transaction
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    nginx.ingress.kubernetes.io/configuration-snippet: |
      more_set_headers "X-Frame-Options: DENY";
      more_set_headers "X-Content-Type-Options: nosniff";
      more_set_headers "X-XSS-Protection: 1; mode=block";
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - audit-trail.gogidix.com
    secretName: audit-trail-service-tls
  rules:
  - host: audit-trail.gogidix.com
    http:
      paths:
      - path: /api/audit(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: audit-trail-service
            port:
              number: 8081
      - path: /
        pathType: Prefix
        backend:
          service:
            name: audit-trail-service
            port:
              number: 8081
