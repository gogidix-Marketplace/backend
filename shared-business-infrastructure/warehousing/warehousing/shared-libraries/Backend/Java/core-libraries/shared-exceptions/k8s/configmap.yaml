apiVersion: v1
kind: ConfigMap
metadata:
  name: shared-exceptions-config
  namespace: shared-libraries
data:
  application.yml: |
    gogidix:
      shared-exceptions:
        enabled: true
        config:
          key: value
