apiVersion: v1
kind: ConfigMap
metadata:
  name: voice-recognition-service-config
  namespace: ai-services
  labels:
    app: voice-recognition-service
    team: team-f-ai-services
data:
  mongodb-host: "mongodb"
  mongodb-port: "27017"
  mongodb-db: "voice-recognition-service"
  redis-host: "redis"
  redis-port: "6379"
  kafka-bootstrap-servers: "kafka:9092"
