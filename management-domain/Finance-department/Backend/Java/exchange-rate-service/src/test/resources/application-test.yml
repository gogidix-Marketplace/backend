# Test Configuration for Exchange Rate Service
spring:
  application:
    name: exchange-rate-service-test
  data:
    mongodb:
      host: localhost
      port: 27017
      database: exchange-rate-test-db
      auto-index-creation: true
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: exchange-rate-test-group
      auto-offset-reset: earliest
spring.redis:
  host: localhost
  port: 6379
  database: 13
server:
  port: 0
logging:
  level:
    com.gogidix.finance.exchangerate: DEBUG
    org.springframework.kafka: WARN
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
test:
  tenant:
    id: tenant-test-001
