apiVersion: v1
kind: ServiceAccount
metadata:
  name: centralized-reporting-service
  namespace: centralized-dashboard
  labels:
    app: centralized-reporting-service
    team: team-f-centralized-dashboard
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: centralized-reporting-service
  namespace: centralized-dashboard
  labels:
    app: centralized-reporting-service
    team: team-f-centralized-dashboard
spec:
  replicas: 2
  selector:
    matchLabels:
      app: centralized-reporting-service
  template:
    metadata:
      labels:
        app: centralized-reporting-service
        team: team-f-centralized-dashboard
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8922"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      serviceAccountName: centralized-reporting-service
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
      - name: centralized-reporting-service
        image: ghcr.io/gogidix/centralized-reporting-service:latest
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 8922
          protocol: TCP
        env:
        - name: DATABASE_URL
          valueFrom:
            configMapKeyRef:
              name: centralized-reporting-service-config
              key: database-url
        - name: DATABASE_USERNAME
          valueFrom:
            secretKeyRef:
              name: centralized-reporting-service-secrets
              key: database-username
        - name: DATABASE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: centralized-reporting-service-secrets
              key: database-password
        - name: REDIS_HOST
          valueFrom:
            configMapKeyRef:
              name: centralized-reporting-service-config
              key: redis-host
        - name: REDIS_PORT
          valueFrom:
            configMapKeyRef:
              name: centralized-reporting-service-config
              key: redis-port
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: centralized-reporting-service-secrets
              key: redis-password
        - name: KAFKA_BOOTSTRAP_SERVERS
          valueFrom:
            configMapKeyRef:
              name: centralized-reporting-service-config
              key: kafka-bootstrap-servers
        - name: REPORT_OUTPUT_DIR
          valueFrom:
            configMapKeyRef:
              name: centralized-reporting-service-config
              key: report-output-dir
        - name: SPRING_PROFILES_ACTIVE
          value: "prod"
        resources:
          requests:
            memory: "768Mi"
            cpu: "250m"
          limits:
            memory: "1.5Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8922
          initialDelaySeconds: 60
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8922
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        startupProbe:
          httpGet:
            path: /actuator/health
            port: 8922
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 18
        volumeMounts:
        - name: tmp
          mountPath: /tmp
        - name: reports
          mountPath: /tmp/reports
      volumes:
      - name: tmp
        emptyDir: {}
      - name: reports
        emptyDir: {}
      restartPolicy: Always
