apiVersion: v1
kind: Service
metadata:
  name: stock-adjustment-service
  namespace: warehousing-core
  labels:
    app: stock-adjustment-service
    service: stock-adjustment-service
spec:
  type: ClusterIP
  sessionAffinity: None
  ports:
  - name: http
    port: 8212
    targetPort: http
    protocol: TCP
  - name: management
    port: 8213
    targetPort: management
    protocol: TCP
  selector:
    app: stock-adjustment-service
---
apiVersion: v1
kind: Service
metadata:
  name: stock-adjustment-service-headless
  namespace: warehousing-core
  labels:
    app: stock-adjustment-service
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - name: http
    port: 8212
    targetPort: http
    protocol: TCP
  selector:
    app: stock-adjustment-service
