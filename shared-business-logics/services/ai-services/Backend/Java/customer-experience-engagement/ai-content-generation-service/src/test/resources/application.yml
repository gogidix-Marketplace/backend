spring:
  application:
    name: ai-content-generation-service-test
  data:
    mongodb:
      uri: mongodb://localhost:27017/ai_content_generation_test?connectTimeoutMS=60000&serverSelectionTimeoutMS=60000&socketTimeoutMS=60000
      auto-index-creation: false
  test:
    context:
      cache:
        enabled: false

server:
  port: 0  # Random port for tests

logging:
  level:
    com.gogidix: DEBUG
    org.springframework.data.mongodb: DEBUG
