# Test Configuration for Forecasting Service
spring:
  application:
    name: forecasting-service-test
  data:
    mongodb:
      host: localhost
      port: 27017
      database: forecasting-test-db
      auto-index-creation: true
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: forecasting-test-group
      auto-offset-reset: earliest
spring.redis:
  host: localhost
  port: 6379
  database: 16
server:
  port: 0
logging:
  level:
    com.gogidix.finance.forecasting: DEBUG
    org.springframework.kafka: WARN
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
test:
  tenant:
    id: tenant-test-001
