apiVersion: v1
kind: Service
metadata:
  name: ai-summarization-service-metrics
  namespace: ai-services
  labels:
    app: ai-summarization-service
    team: team-ai-content-processing
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "8080"
    prometheus.io/path: "/actuator/prometheus"
spec:
  type: ClusterIP
  ports:
  - name: metrics
    port: 8080
    targetPort: management
    protocol: TCP
  selector:
    app: ai-summarization-service
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ai-summarization-service
  namespace: ai-services
  labels:
    app: ai-summarization-service
    team: team-ai-content-processing
    release: prometheus-operator
spec:
  selector:
    matchLabels:
      app: ai-summarization-service
  endpoints:
  - port: metrics
    path: /actuator/prometheus
    interval: 30s
    scrapeTimeout: 10s
  namespaceSelector:
    matchNames:
    - ai-services
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: ai-summarization-service-alerts
  namespace: ai-services
  labels:
    app: ai-summarization-service
    team: team-ai-content-processing
data:
  alerts.yml: |
    groups:
    - name: ai-summarization-service
      rules:
      - alert: AISummarizationServiceDown
        expr: up{job="ai-summarization-service"} == 0
        for: 5m
        labels:
          severity: critical
          team: team-ai-content-processing
        annotations:
          summary: "AI Summarization Service is down"
          description: "AI Summarization Service has been down for more than 5 minutes"
      - alert: AISummarizationServiceHighErrorRate
        expr: rate(http_requests_total{job="ai-summarization-service",status=~"5.."}[5m]) > 0.05
        for: 5m
        labels:
          severity: warning
          team: team-ai-content-processing
        annotations:
          summary: "High error rate on AI Summarization Service"
          description: "Error rate is {{ $value }} errors per second"
      - alert: AISummarizationServiceHighMemory
        expr: jvm_memory_used_bytes{job="ai-summarization-service",area="heap"} / jvm_memory_max_bytes{job="ai-summarization-service",area="heap"} > 0.9
        for: 10m
        labels:
          severity: warning
          team: team-ai-content-processing
        annotations:
          summary: "High memory usage on AI Summarization Service"
          description: "Heap memory usage is above 90%"
