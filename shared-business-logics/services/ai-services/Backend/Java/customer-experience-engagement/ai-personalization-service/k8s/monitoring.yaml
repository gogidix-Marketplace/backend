apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ai-personalization-service
  namespace: ai-services
  labels:
    app: ai-personalization-service
    team: team-ai-customer-experience
spec:
  selector:
    matchLabels:
      app: ai-personalization-service
  endpoints:
  - port: http
    path: /actuator/prometheus
    interval: 30s
    scrapeTimeout: 10s
