---
apiVersion: v1
kind: ConfigMap
metadata:
  name: dlp-service-config
  namespace: infrastructure
  labels:
    app: dlp-service
    version: v1
data:
  application.yml: |
    server:
      port: 8080
    spring:
      application:
        name: dlp-service
      cloud:
        discovery:
          enabled: true
          service-url:
            defaultZone: http://discovery-service:8761/eureka/
        config:
          enabled: true
          uri: http://config-server:8888
          fail-fast: true
          retry:
            initial-interval: 2000
            multiplier: 1.5
            max-interval: 60000
            max-attempts: 6
      data:
        mongodb:
          uri: mongodb://dlp-user:${MONGODB_PASSWORD}@mongodb.infrastructure.svc.cluster.local:27017/dlp-service?authSource=admin
          auto-index-creation: true
    management:
      endpoints:
        web:
          exposure:
            include: health,info,metrics,prometheus
      endpoint:
        health:
          show-details: always
      metrics:
        export:
          prometheus:
            enabled: true
---
apiVersion: v1
kind: Secret
metadata:
  name: dlp-service-secrets
  namespace: infrastructure
  labels:
    app: dlp-service
type: Opaque
stringData:
  mongodb-password: "ChangeThisInProduction123!"
  jwt-secret: "ChangeThisJwtSecretInProduction"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dlp-service
  namespace: infrastructure
  labels:
    app: dlp-service
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: dlp-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: dlp-service
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      serviceAccountName: dlp-service
      containers:
        - name: dlp-service
          image: gogidix/dlp-service:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
            - name: management
              containerPort: 8080
              protocol: TCP
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
            - name: MONGODB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: dlp-service-secrets
                  key: mongodb-password
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: dlp-service-secrets
                  key: jwt-secret
            - name: EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE
              value: "http://discovery-service:8761/eureka/"
            - name: SPRING_CLOUD_CONFIG_URI
              value: "http://config-server:8888"
            - name: MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE
              value: "health,info,metrics,prometheus"
          envFrom:
            - configMapRef:
                name: dlp-service-config
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          volumeMounts:
            - name: config-volume
              mountPath: /app/config
              readOnly: true
      volumes:
        - name: config-volume
          configMap:
            name: dlp-service-config
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - dlp-service
                topologyKey: kubernetes.io/hostname
---
apiVersion: v1
kind: Service
metadata:
  name: dlp-service
  namespace: infrastructure
  labels:
    app: dlp-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: dlp-service
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: dlp-service
  namespace: infrastructure
  labels:
    app: dlp-service
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: dlp-service-hpa
  namespace: infrastructure
  labels:
    app: dlp-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: dlp-service
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: dlp-service-pdb
  namespace: infrastructure
  labels:
    app: dlp-service
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: dlp-service
