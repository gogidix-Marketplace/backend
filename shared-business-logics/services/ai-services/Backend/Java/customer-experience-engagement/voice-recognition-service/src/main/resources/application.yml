spring:
  application:
    name: voice-recognition-service
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8087

logging:
  level:
    com.gogidix.aiservices.voicerecognitionservice: INFO
