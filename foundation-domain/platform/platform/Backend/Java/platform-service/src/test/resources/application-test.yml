spring:
  application:
    name: platform-service

  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

  redis:
    host: localhost
    port: 6370
    database: 1

server:
  port: 8081

logging:
  level:
    root: INFO
    com.gogidix: DEBUG
