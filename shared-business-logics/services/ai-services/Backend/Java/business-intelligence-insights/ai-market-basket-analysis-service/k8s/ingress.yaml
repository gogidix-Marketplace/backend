apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ai-market-basketation-service-ingress
  namespace: ai-services
  labels:
    app: ai-market-basketation-service
    team: team-f-ai-services
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    nginx.ingress.kubernetes.io/configuration-snippet: |
      more_set_headers "X-Frame-Options: SAMEORIGIN";
      more_set_headers "X-Content-Type-Options: nosniff";
    nginx.ingress.kubernetes.io/compression-backend: "on"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - ai-market-basketation.gogidix.com
    secretName: ai-market-basketation-service-tls
  rules:
  - host: ai-market-basketation.gogidix.com
    http:
      paths:
      - path: /api/segmentation(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: ai-market-basketation-service
            port:
              number: 8200
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ai-market-basketation-service
            port:
              number: 8200
