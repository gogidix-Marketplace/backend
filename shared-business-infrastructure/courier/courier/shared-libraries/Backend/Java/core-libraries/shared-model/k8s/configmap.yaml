apiVersion: v1
kind: ConfigMap
metadata:
  name: shared-model-config
  namespace: shared-libraries
data:
  application.yml: |
    gogidix:
      shared-model:
        enabled: true
        config:
          key: value
