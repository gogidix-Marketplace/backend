apiVersion: v1
kind: Service
metadata:
  name: ai-authentication-service-metrics
  namespace: ai-services
  labels:
    app: ai-authentication-service
    app.kubernetes.io/name: ai-authentication-service
    app.kubernetes.io/component: metrics
    app.kubernetes.io/part-of: ai-services
    team: team-ai-security
    domain: security-fraud-prevention
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "8082"
    prometheus.io/path: "/actuator/prometheus"
    prometheus.io/scheme: "http"
spec:
  type: ClusterIP
  ports:
  - name: metrics
    port: 8082
    targetPort: 8082
    protocol: TCP
  selector:
    app: ai-authentication-service
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ai-authentication-service
  namespace: ai-services
  labels:
    app: ai-authentication-service
    app.kubernetes.io/name: ai-authentication-service
    app.kubernetes.io/component: servicemonitor
    app.kubernetes.io/part-of: ai-services
    team: team-ai-security
    domain: security-fraud-prevention
    release: prometheus
  annotations:
    description: "Prometheus ServiceMonitor for AI Authentication Service metrics"
spec:
  selector:
    matchLabels:
      app: ai-authentication-service
      app.kubernetes.io/name: ai-authentication-service
  endpoints:
  - port: metrics
    path: /actuator/prometheus
    scheme: http
    interval: 30s
    scrapeTimeout: 10s
    relabelings:
    - sourceLabels: [__meta_kubernetes_pod_name]
      targetLabel: pod
      action: replace
    - sourceLabels: [__meta_kubernetes_pod_node_name]
      targetLabel: node
      action: replace
    - sourceLabels: [__meta_kubernetes_namespace]
      targetLabel: namespace
      action: replace
    metricRelabelings:
    - sourceLabels: [__name__]
      regex: 'jvm_.*|http_.*|process_.*|system_.*|spring_.*|application_.*'
      action: keep
  namespaceSelector:
    matchNames:
    - ai-services
---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: ai-authentication-service
  namespace: ai-services
  labels:
    app: ai-authentication-service
    app.kubernetes.io/component: podmonitor
    team: team-ai-security
    release: prometheus
spec:
  selector:
    matchLabels:
      app: ai-authentication-service
  podMetricsEndpoints:
  - port: http
    path: /actuator/prometheus
    interval: 30s
    scrapeTimeout: 10s
    relabelings:
    - sourceLabels: [__meta_kubernetes_pod_name]
      targetLabel: pod
    - sourceLabels: [__meta_kubernetes_pod_node_name]
      targetLabel: node
  namespaceSelector:
    matchNames:
    - ai-services
---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: ai-authentication-service
  namespace: ai-services
  labels:
    app: ai-authentication-service
    app.kubernetes.io/component: prometheus-rule
    team: team-ai-security
    release: prometheus
    role: alert-rules
spec:
  groups:
  - name: ai-authentication-service
    interval: 30s
    rules:
    - alert: AAIAuthenticationServiceHighErrorRate
      expr: rate(http_server_requests_seconds_count{job="ai-authentication-service",status=~"5.."}[5m]) > 10
      for: 5m
      labels:
        severity: warning
        team: team-ai-security
      annotations:
        summary: "High error rate for AI Authentication Service"
        description: "Error rate is {{ $value }} errors/sec for the last 5 minutes"
    - alert: AAIAuthenticationServiceHighLatency
      expr: histogram_quantile(0.95, rate(http_server_requests_seconds_bucket{job="ai-authentication-service"}[5m])) > 1
      for: 10m
      labels:
        severity: warning
        team: team-ai-security
      annotations:
        summary: "High latency for AI Authentication Service"
        description: "P95 latency is {{ $value }} seconds"
    - alert: AAIAuthenticationServicePodDown
      expr: up{job="ai-authentication-service"} == 0
      for: 5m
      labels:
        severity: critical
        team: team-ai-security
      annotations:
        summary: "AI Authentication Service pod is down"
        description: "Pod {{ $labels.instance }} has been down for more than 5 minutes"
    - alert: AAIAuthenticationServiceHighMemoryUsage
      expr: jvm_memory_used_bytes{job="ai-authentication-service",area="heap"} / jvm_memory_max_bytes{job="ai-authentication-service",area="heap"} > 0.9
      for: 10m
      labels:
        severity: warning
        team: team-ai-security
      annotations:
        summary: "High memory usage for AI Authentication Service"
        description: "Heap memory usage is {{ $value | humanizePercentage }}"
    - alert: AAIAuthenticationServiceHighCPUUsage
      expr: rate(process_cpu_seconds_total{job="ai-authentication-service"}[5m]) > 0.8
      for: 15m
      labels:
        severity: warning
        team: team-ai-security
      annotations:
        summary: "High CPU usage for AI Authentication Service"
        description: "CPU usage is {{ $value | humanizePercentage }}"
