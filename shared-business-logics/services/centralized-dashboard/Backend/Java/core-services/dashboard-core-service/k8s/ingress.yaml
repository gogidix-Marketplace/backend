apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dashboard-core-service-ingress
  namespace: centralized-dashboard
  labels:
    app: dashboard-core-service
    team: team-f-centralized-dashboard
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    nginx.ingress.kubernetes.io/configuration-snippet: |
      more_set_headers "X-Frame-Options: SAMEORIGIN";
      more_set_headers "X-Content-Type-Options: nosniff";
    nginx.ingress.kubernetes.io/compression-backend: "on"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - dashboard-core.gogidix.com
    secretName: dashboard-core-service-tls
  rules:
  - host: dashboard-core.gogidix.com
    http:
      paths:
      - path: /api/dashboard(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: dashboard-core-service
            port:
              number: 8904
      - path: /
        pathType: Prefix
        backend:
          service:
            name: dashboard-core-service
            port:
              number: 8904
