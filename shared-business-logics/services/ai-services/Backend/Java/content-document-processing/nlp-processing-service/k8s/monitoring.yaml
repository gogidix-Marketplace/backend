apiVersion: v1
kind: ServiceMonitor
metadata:
  name: nlp-processing-service
  namespace: ai-services
  labels:
    app: nlp-processing-service
    version: v1
    team: team-ai-content-processing
    component: content-document-processing
    environment: production
    release: prometheus-operator
spec:
  selector:
    matchLabels:
      app: nlp-processing-service
  endpoints:
  - port: management
    path: /actuator/prometheus
    interval: 30s
    scrapeTimeout: 10s
    scheme: http
  namespaceSelector:
    matchNames:
    - ai-services
---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: nlp-processing-service
  namespace: ai-services
  labels:
    app: nlp-processing-service
    version: v1
    team: team-ai-content-processing
    component: content-document-processing
    environment: production
    release: prometheus-operator
spec:
  groups:
  - name: nlp-processing-service
    interval: 30s
    rules:
    - alert: NlpProcessingServiceHighErrorRate
      expr: rate(http_server_requests_seconds_count{app="nlp-processing-service",status=~"5.."}[5m]) > 0.05
      for: 5m
      labels:
        severity: warning
        team: team-ai-content-processing
      annotations:
        summary: High error rate detected for NLP Processing Service
        description: "Error rate is {{ $value }} errors/sec"
    - alert: NlpProcessingServiceHighLatency
      expr: histogram_quantile(0.95, rate(http_server_requests_seconds_bucket{app="nlp-processing-service"}[5m])) > 1
      for: 10m
      labels:
        severity: warning
        team: team-ai-content-processing
      annotations:
        summary: High latency detected for NLP Processing Service
        description: "P95 latency is {{ $value }} seconds"
    - alert: NlpProcessingServicePodNotReady
      expr: kube_pod_status_ready{app="nlp-processing-service",namespace="ai-services"} == 0
      for: 5m
      labels:
        severity: critical
        team: team-ai-content-processing
      annotations:
        summary: NLP Processing Service pod not ready
        description: "Pod {{ $labels.pod }} is not ready"
    - alert: NlpProcessingServiceHighMemoryUsage
      expr: rate(container_memory_working_set_bytes{app="nlp-processing-service"}[5m]) > 1200000000
      for: 10m
      labels:
        severity: warning
        team: team-ai-content-processing
      annotations:
        summary: High memory usage detected for NLP Processing Service
        description: "Memory usage is {{ $value }} bytes"
    - alert: NlpProcessingServiceHighCpuUsage
      expr: rate(container_cpu_usage_seconds_total{app="nlp-processing-service"}[5m]) > 0.8
      for: 10m
      labels:
        severity: warning
        team: team-ai-content-processing
      annotations:
        summary: High CPU usage detected for NLP Processing Service
        description: "CPU usage is {{ $value }} cores"
