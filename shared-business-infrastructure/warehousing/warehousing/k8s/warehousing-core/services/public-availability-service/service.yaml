apiVersion: v1
kind: Service
metadata:
  name: public-availability-service
  namespace: warehousing-core
  labels:
    app: public-availability-service
    service: public-availability-service
spec:
  type: ClusterIP
  sessionAffinity: None
  ports:
  - name: http
    port: 8282
    targetPort: http
    protocol: TCP
  - name: management
    port: 8283
    targetPort: management
    protocol: TCP
  selector:
    app: public-availability-service
---
apiVersion: v1
kind: Service
metadata:
  name: public-availability-service-headless
  namespace: warehousing-core
  labels:
    app: public-availability-service
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - name: http
    port: 8282
    targetPort: http
    protocol: TCP
  selector:
    app: public-availability-service
