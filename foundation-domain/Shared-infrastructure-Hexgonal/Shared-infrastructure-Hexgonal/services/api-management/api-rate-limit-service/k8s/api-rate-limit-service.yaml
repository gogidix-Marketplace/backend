---
apiVersion: v1
kind: ConfigMap
metadata:
  name: api-rate-limit-service-config
  namespace: infrastructure
  labels:
    app: api-rate-limit-service
    version: v1
data:
  application.yml: |
    server:
      port: 8080
    spring:
      application:
        name: api-rate-limit-service
      cloud:
        discovery:
          enabled: true
          service-url:
            defaultZone: http://discovery-service:8761/eureka/
        config:
          enabled: true
          uri: http://config-server:8888
      data:
        redis:
          host: redis-sentinel-cluster.infrastructure.svc.cluster.local
          port: 26379
          password: ${REDIS_PASSWORD}
          sentinel:
            master: mymaster
    management:
      endpoints:
        web:
          exposure:
            include: health,info,metrics,prometheus
      endpoint:
        health:
          show-details: always
      metrics:
        export:
          prometheus:
            enabled: true
    api-rate-limit:
      default-requests-per-minute: 100
      default-requests-per-hour: 2000
      window-size-seconds: 60
---
apiVersion: v1
kind: Secret
metadata:
  name: api-rate-limit-service-secrets
  namespace: infrastructure
  labels:
    app: api-rate-limit-service
type: Opaque
stringData:
  redis-password: "ChangeThisInProduction123!"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-rate-limit-service
  namespace: infrastructure
  labels:
    app: api-rate-limit-service
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-rate-limit-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: api-rate-limit-service
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      serviceAccountName: api-rate-limit-service
      containers:
        - name: api-rate-limit-service
          image: gogidix/api-rate-limit-service:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: api-rate-limit-service-secrets
                  key: redis-password
            - name: EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE
              value: "http://discovery-service:8761/eureka/"
            - name: SPRING_CLOUD_CONFIG_URI
              value: "http://config-server:8888"
          envFrom:
            - configMapRef:
                name: api-rate-limit-service-config
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - api-rate-limit-service
                topologyKey: kubernetes.io/hostname
---
apiVersion: v1
kind: Service
metadata:
  name: api-rate-limit-service
  namespace: infrastructure
  labels:
    app: api-rate-limit-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: api-rate-limit-service
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: api-rate-limit-service
  namespace: infrastructure
  labels:
    app: api-rate-limit-service
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: api-rate-limit-service-hpa
  namespace: infrastructure
  labels:
    app: api-rate-limit-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: api-rate-limit-service
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: api-rate-limit-service-pdb
  namespace: infrastructure
  labels:
    app: api-rate-limit-service
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: api-rate-limit-service
