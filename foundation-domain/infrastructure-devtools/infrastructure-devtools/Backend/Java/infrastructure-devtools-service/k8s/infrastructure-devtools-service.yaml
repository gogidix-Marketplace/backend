---
apiVersion: v1
kind: ConfigMap
metadata:
  name: infrastructure-devtools-service-config
  namespace: infra-devtools
  labels:
    app: infrastructure-devtools-service
    version: v1
data:
  application.yml: |
    server:
      port: 8400
    spring:
      application:
        name: infrastructure-devtools-service
      cloud:
        discovery:
          enabled: true
          service-url:
            defaultZone: http://discovery-service.infrastructure.svc.cluster.local:8761/eureka/
        config:
          enabled: true
          uri: http://config-server.centralized-config.svc.cluster.local:8210
          fail-fast: true
      datasource:
        url: jdbc:postgresql://postgresql.infrastructure.svc.cluster.local:5432/devtools-db
        username: devtools-user
        driver-class-name: org.postgresql.Driver
      jpa:
        hibernate:
          ddl-auto: update
        show-sql: false
        properties:
          hibernate:
            dialect: org.hibernate.dialect.PostgreSQLDialect
    management:
      endpoints:
        web:
          exposure:
            include: health,info,metrics,prometheus
      endpoint:
        health:
          show-details: always
      metrics:
        export:
          prometheus:
            enabled: true
---
apiVersion: v1
kind: Secret
metadata:
  name: infrastructure-devtools-service-secrets
  namespace: infra-devtools
  labels:
    app: infrastructure-devtools-service
type: Opaque
stringData:
  postgresql-password: "ChangeThisInProduction123!"
  jwt-secret: "ChangeThisJwtSecretInProduction"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: infrastructure-devtools-service
  namespace: infra-devtools
  labels:
    app: infrastructure-devtools-service
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: infrastructure-devtools-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: infrastructure-devtools-service
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8400"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      serviceAccountName: infrastructure-devtools-service
      containers:
        - name: infrastructure-devtools-service
          image: gogidix/infrastructure-devtools-service:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8400
              protocol: TCP
            - name: management
              containerPort: 8400
              protocol: TCP
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: infrastructure-devtools-service-secrets
                  key: postgresql-password
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: infrastructure-devtools-service-secrets
                  key: jwt-secret
            - name: EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE
              value: "http://discovery-service.infrastructure.svc.cluster.local:8761/eureka/"
            - name: SPRING_CLOUD_CONFIG_URI
              value: "http://config-server.centralized-config.svc.cluster.local:8210"
            - name: MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE
              value: "health,info,metrics,prometheus"
          envFrom:
            - configMapRef:
                name: infrastructure-devtools-service-config
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8400
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8400
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: infrastructure-devtools-service
  namespace: infra-devtools
  labels:
    app: infrastructure-devtools-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8400
      targetPort: 8400
      protocol: TCP
  selector:
    app: infrastructure-devtools-service
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: infrastructure-devtools-service
  namespace: infra-devtools
  labels:
    app: infrastructure-devtools-service
