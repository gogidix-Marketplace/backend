# Test Configuration for Consolidation Service
spring:
  application:
    name: consolidation-service-test
  data:
    mongodb:
      host: localhost
      port: 27017
      database: consolidation-test-db
      auto-index-creation: true
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: consolidation-test-group
      auto-offset-reset: earliest
spring.redis:
  host: localhost
  port: 6379
  database: 10
server:
  port: 0
logging:
  level:
    com.gogidix.finance.consolidation: DEBUG
    org.springframework.kafka: WARN
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
test:
  tenant:
    id: tenant-test-001
