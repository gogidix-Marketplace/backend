apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: data-aggregation-service-ingress
  namespace: centralized-dashboard
  labels:
    app: data-aggregation-service
    team: team-f-centralized-dashboard
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    nginx.ingress.kubernetes.io/configuration-snippet: |
      more_set_headers "X-Frame-Options: SAMEORIGIN";
      more_set_headers "X-Content-Type-Options: nosniff";
    nginx.ingress.kubernetes.io/compression-backend: "on"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - data-aggregation.gogidix.com
    secretName: data-aggregation-service-tls
  rules:
  - host: data-aggregation.gogidix.com
    http:
      paths:
      - path: /api/aggregation(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: data-aggregation-service
            port:
              number: 8905
      - path: /
        pathType: Prefix
        backend:
          service:
            name: data-aggregation-service
            port:
              number: 8905
