apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ai-personalization-service-ingress
  namespace: ai-services
  labels:
    app: ai-personalization-service
    team: team-ai-customer-experience
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    nginx.ingress.kubernetes.io/configuration-snippet: |
      more_set_headers "X-Frame-Options: SAMEORIGIN";
      more_set_headers "X-Content-Type-Options: nosniff";
    nginx.ingress.kubernetes.io/compression-backend: "on"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - ai-personalization.gogidix.com
    secretName: ai-personalization-service-tls
  rules:
  - host: ai-personalization.gogidix.com
    http:
      paths:
      - path: /api/personalization(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: ai-personalization-service
            port:
              number: 8080
