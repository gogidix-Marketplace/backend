spring:
  data:
    redis:
      host: localhost
      port: 6379
      database: 0

  kafka:
    bootstrap-servers: localhost:9092

server:
  port: 8080

logging:
  level:
    root: DEBUG
    com.gogidix: DEBUG
    org.springframework: DEBUG
    org.springframework.data.redis: DEBUG
