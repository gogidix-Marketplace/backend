apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: status-broadcast-service
  namespace: transaction-orchestration
  labels:
    app: status-broadcast-service
    team: team-b-transaction
spec:
  selector:
    matchLabels:
      app: status-broadcast-service
  endpoints:
  - port: http
    path: /actuator/prometheus
    interval: 30s
    scrapeTimeout: 10s
    scheme: http
