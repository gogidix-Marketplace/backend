spring:
  application:
    name: webhook-management-service
  data:
    mongodb:
      uri: mongodb://localhost:27017
      database: gogidix_webhook_service
  redis:
    host: localhost
    port: 6379
    database: 10
  kafka:
    bootstrap-servers: localhost:9092

server:
  port: 8100

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.gogidix: DEBUG
    org.springframework.data.mongodb: DEBUG
