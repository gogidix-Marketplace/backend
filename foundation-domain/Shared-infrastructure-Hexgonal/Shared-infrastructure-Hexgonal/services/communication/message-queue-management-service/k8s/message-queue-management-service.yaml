---
apiVersion: v1
kind: ConfigMap
metadata:
  name: message-queue-management-service-config
  namespace: infrastructure
  labels:
    app: message-queue-management-service
    version: v1
data:
  application.yml: |
    server:
      port: 8080
    spring:
      application:
        name: message-queue-management-service
      cloud:
        discovery:
          enabled: true
          service-url:
            defaultZone: http://discovery-service:8761/eureka/
        config:
          enabled: true
          uri: http://config-server:8888
      data:
        mongodb:
          uri: mongodb://mq-user:${MONGODB_PASSWORD}@mongodb.infrastructure.svc.cluster.local:27017/message-queue-management?authSource=admin
          auto-index-creation: true
        kafka:
          bootstrap-servers: kafka-bootstrap.infrastructure.svc.cluster.local:9092
    management:
      endpoints:
        web:
          exposure:
            include: health,info,metrics,prometheus
      endpoint:
        health:
          show-details: always
      metrics:
        export:
          prometheus:
            enabled: true
---
apiVersion: v1
kind: Secret
metadata:
  name: message-queue-management-service-secrets
  namespace: infrastructure
  labels:
    app: message-queue-management-service
type: Opaque
stringData:
  mongodb-password: "ChangeThisInProduction123!"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: message-queue-management-service
  namespace: infrastructure
  labels:
    app: message-queue-management-service
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: message-queue-management-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: message-queue-management-service
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      serviceAccountName: message-queue-management-service
      containers:
        - name: message-queue-management-service
          image: gogidix/message-queue-management-service:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
            - name: MONGODB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: message-queue-management-service-secrets
                  key: mongodb-password
            - name: EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE
              value: "http://discovery-service:8761/eureka/"
            - name: SPRING_CLOUD_CONFIG_URI
              value: "http://config-server:8888"
          envFrom:
            - configMapRef:
                name: message-queue-management-service-config
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - message-queue-management-service
                topologyKey: kubernetes.io/hostname
---
apiVersion: v1
kind: Service
metadata:
  name: message-queue-management-service
  namespace: infrastructure
  labels:
    app: message-queue-management-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: message-queue-management-service
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: message-queue-management-service
  namespace: infrastructure
  labels:
    app: message-queue-management-service
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: message-queue-management-service-hpa
  namespace: infrastructure
  labels:
    app: message-queue-management-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: message-queue-management-service
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: message-queue-management-service-pdb
  namespace: infrastructure
  labels:
    app: message-queue-management-service
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: message-queue-management-service
