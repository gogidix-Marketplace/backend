# Test Configuration for Cash Flow Service
spring:
  application:
    name: cashflow-service-test
  data:
    mongodb:
      host: localhost
      port: 27017
      database: cashflow-test-db
      auto-index-creation: true
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: cashflow-test-group
      auto-offset-reset: earliest
spring.redis:
  host: localhost
  port: 6379
  database: 8
server:
  port: 0
logging:
  level:
    com.gogidix.finance.cashflow: DEBUG
    org.springframework.kafka: WARN
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
test:
  tenant:
    id: tenant-test-001
