server:
  port: 8091

spring:
  application:
    name: shared-infrastructure-api-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        - id: gateway-service-route
          uri: lb://gateway-service
          predicates:
            - Path=/api/gateway/**
          filters:
            - name: AuthenticationFilter
            - name: RateLimitFilter
            - name: RequestLoggingFilter

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,gateway