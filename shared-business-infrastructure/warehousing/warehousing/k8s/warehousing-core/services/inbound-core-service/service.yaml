apiVersion: v1
kind: Service
metadata:
  name: inbound-core-service
  namespace: warehousing-core
  labels:
    app: inbound-core-service
    service: inbound-core-service
spec:
  type: ClusterIP
  sessionAffinity: None
  ports:
  - name: http
    port: 8200
    targetPort: http
    protocol: TCP
  - name: management
    port: 8201
    targetPort: management
    protocol: TCP
  selector:
    app: inbound-core-service
---
apiVersion: v1
kind: Service
metadata:
  name: inbound-core-service-headless
  namespace: warehousing-core
  labels:
    app: inbound-core-service
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - name: http
    port: 8200
    targetPort: http
    protocol: TCP
  selector:
    app: inbound-core-service
