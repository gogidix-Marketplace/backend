apiVersion: v1
kind: ConfigMap
metadata:
  name: shared-validation-config
  namespace: shared-libraries
data:
  application.yml: |
    gogidix:
      shared-validation:
        enabled: true
        config:
          key: value
