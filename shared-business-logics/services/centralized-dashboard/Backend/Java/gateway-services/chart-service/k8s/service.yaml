apiVersion: v1
kind: Service
metadata:
  name: chart-service
  namespace: centralized-dashboard
  labels:
    app: chart-service
    team: team-f-centralized-dashboard
spec:
  type: ClusterIP
  selector:
    app: chart-service
  ports:
  - name: http
    port: 8909
    targetPort: 8909
    protocol: TCP
  sessionAffinity: None
